{
    "id": "validate_bst",
    "title": "Validate Binary Search Tree",
    "description": "Given the <code>root</code> of a binary tree, determine if it is a valid binary search tree (BST). A BST is defined as follows: the left subtree of a node contains only nodes with keys less than the node's key, and the right subtree of a node contains only nodes with keys greater than the node's key. Both the left and right subtrees must also be binary search trees.",
    "difficulty": "Medium",
    "category": "Tree, Binary Search Tree",
    "examples": [
        {
            "input": "[2, 1, 3]",
            "output": "true",
            "explanation": "The tree structure is valid since 1 < 2 < 3."
        },
        {
            "input": "[5, 1, 4, null, null, 3, 6]",
            "output": "false",
            "explanation": "The left child (4) is greater than the root (5) which violates the BST property."
        },
        {
            "input": "[10, 5, 15, null, null, 6, 20]",
            "output": "false",
            "explanation": "The left subtree has a value (6) that is greater than the root (5), violating the BST condition."
        }
    ],
    "constraints": [
        "The number of nodes in the tree is in the range [0, 10^4].",
        "Each node's value is in the range [-10^4, 10^4]."
    ],
    "tags": [
        "Tree",
        "Binary Search Tree",
        "Recursion"
    ],
    "companies": [
        "Google",
        "Amazon",
        "Microsoft",
        "Apple"
    ],
    "similar_problems": [
        "is_bst",
        "lowest_common_ancestor",
        "inorder_successor"
    ],
    "hints": [
        "Consider using recursion to traverse the tree.",
        "Keep track of the valid range for each node's value.",
        "Check both left and right subtrees recursively.",
        "Use an iterative approach with a stack if recursion is not preferred."
    ],
    "created_at": "2024-01-15T10:30:00Z",
    "updated_at": "2024-01-15T10:30:00Z",
    "version": "1.0"
}